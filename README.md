# BLOG-WEBSITE

A full-stack blog website project with separate **frontend** (React) and **backend** (Node.js/Express) folders. This project allows users to read, create, and manage blogs with authentication support.

## Project Structure

BLOG-WEBSITE/
├─ frontend/ # React frontend
│ ├─ public/ # HTML, favicon, manifest
│ └─ src/ # React components, pages, styles
├─ backend/ # Node.js/Express backend
│ └─ ... # Backend APIs and logic
├─ README.md # Project documentation

yaml
Copy code

## Features

- User authentication (Register/Login)
- Create, read, update, delete blog posts
- Responsive design with React
- Backend APIs using Node.js and Express
- Easy to extend for additional features

## Frontend Setup

1. Navigate to the frontend folder:

bash
cd frontend
Install dependencies:

bash
Copy code
npm install
Start the development server:

bash
Copy code
npm start
Open http://localhost:3000 to view in the browser.

Backend Setup
Navigate to the backend folder:

bash
Copy code
cd backend
Install dependencies:

bash
Copy code
npm install
Start the server:

bash
Copy code
node index.js
Backend runs on http://localhost:5000 (or your configured port).

Notes
Do not commit node_modules/. Use .gitignore to ignore it.

Ensure frontend .git is removed before adding to root repo to avoid submodule issues.

Contributing
Fork the repository

Create a new branch

Make your changes and commit

Push your branch and create a Pull Request

License
This project is open-source and free to use.


Copy code
Aap isse **`BLOG-WEBSITE/README.md`** me paste kar do, commit & push kar do.  

